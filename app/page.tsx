import StatusCard from "@/components/StatusCard";

const Home = () => {
  return (
    <div className="flex flex-wrap flex-col md:flex-row justify-center items-center h-screen">
      <StatusCard />
    </div>
  );
};

export default Home;
